@model PagedList.IPagedList<Application.eGreeting.Models.Card>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>
@if (ViewBag.Message != null)
{
    <span class="success sweet-alert"> @ViewBag.Message</span>
}
@using PagedList.Mvc;
@using PagedList;

    <table class="table">
        @foreach (var item in Model)
        {
            @*<tr>

                <td>
                    @Html.DisplayFor(modelItem => item.NameCard)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Category)
                </td>
                <td width="20%">
                    <img src="~/ImageCard/@item.ImageName" alt="@item.ImageName" style="width:100%" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateCreated)
                </td>
                <td>
                    @Html.ActionLink("Select", "CreateTrans", "User", new { id = item.CardId }, new { @class = "btn btn-primary" })
                </td>
            </tr>*@

            <div class="col-lg-4 col-md-4 col-sm-4 col-xs-12">
                <div class="box-card">
                    <img src="~/ImageCard/@item.ImageName" alt="image" class="image" style="width:100%">
                    <div class="middle">
                        <a href="#" class="btn-select">SELECT</a>
                    </div>
                    <div class="name-card">
                        <h5>@Html.DisplayFor(modelItem => item.NameCard)</h5>
                    </div>
                </div>
            </div>

            @*<div class="col-lg-4 col-md-4 col-sm-4 col-xs-12">
                <div class="rle_property_infobox">
                    <div class="row">
                        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                            <div class="rle_property_infobox_img">
                                <img src="~/ImageCard/@item.ImageName" alt="" style="width:100%">
                            </div>
                        </div>
                        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                            <div class="rle_property_infobox_details">
                                <h5>@Html.DisplayFor(modelItem => item.NameCard)</h5>
                                <a href="#" class="btn-select">SELECT</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>*@
        }

    </table>

@Html.PagedListPager(Model, page => Url.Action("Index", new { page, @class = "page-link" }), new PagedListRenderOptions()
   {
       ContainerDivClasses = new [] { "Page", "navigation" },
       LiElementClasses = new List<string> { "page-item" }
   })


